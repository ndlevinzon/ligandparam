# Upstream scission tree

The **runtime Python package** used by ligandparam / ffpopt now lives at
[`src/scission`](../src/scission).

This directory keeps the original docs, examples, and tests. Prefer importing
``scission`` from ``src/scission`` (installed via the ligandparam
``pyproject.toml``) rather than installing this tree separately.
